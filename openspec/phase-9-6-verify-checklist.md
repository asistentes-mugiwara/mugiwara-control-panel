# Phase 9.6 verify checklist

- [x] `npm --prefix apps/web run typecheck`
- [x] `npm --prefix apps/web run build`
- [x] `git diff --check`

## Notes
- La fase es frontend/fixture-driven y se centra en layout editorial y navegación allowlisted.
- No se añadió backend real de vault en esta iteración.
