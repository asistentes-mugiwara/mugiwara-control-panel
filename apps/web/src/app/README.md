# app shell

Aquí vive el shell principal del frontend Next.js.

- La microfase 8.1 dejó el runtime mínimo (`layout`, redirección raíz y `dashboard` placeholder).
- La microfase 8.2 añade el shell navegable con sus 6 rutas canónicas y placeholders finos por módulo.
- Las siguientes microfases deben meter contenido real de lectura sin reconstruir esta base global.
