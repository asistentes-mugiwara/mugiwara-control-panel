# app shell

Aquí vive el arranque del frontend Next.js.

La microfase 8.1 deja preparado el runtime mínimo (`layout`, redirección raíz y `dashboard` placeholder) para que la siguiente microfase construya el shell real de navegación.
